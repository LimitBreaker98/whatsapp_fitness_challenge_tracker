# Backend Environment Variables
# Copy this file to .env and fill in your values

# API Key for authenticating POST /api/update requests
# Generate a secure key with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Default (dev only): dev-secret-key
API_KEY=your-secure-api-key-here

# Data directory for JSON storage (for production deployment)
# Default: same directory as storage.py
DATA_DIR=/data

# Allowed origins for CORS (comma-separated for multiple origins)
# Default: http://localhost:5173
ALLOWED_ORIGINS=https://your-app.vercel.app
